import React from 'react'

const Filter = () => {
  return (
    <div>
      {filterData.map((data)=>
    
    {
        <button>
            {data.title};
                    </button>
    })}
    </div>
  )
}

export default Filter
